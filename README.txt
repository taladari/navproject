==================README======================

Program to read, format and filter csv files with wifi network data to csv/kml files.

The program reads csv files from a given folder path and format them into a new csv file.
Then the program filters the data by a filter given by the user (time, id, location), and outputs the data into a KML file that can be viewed through google earth.

